// specs/design/security.json → roles[].name, in that file's order. The first
// is who a visitor is with no ?role= on the URL — Viewer is this design's
// coldStartRole, the base read-only experience.
export const mockRoles = ["Viewer", "Admin"];
